{
  "name": "smarttrim",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "start": "electron-vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "pack": "electron-vite build && electron-builder --win"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "@xmldom/xmldom": "^0.9.12",
    "electron": "^44.3.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "onnxruntime-node": "^1.29.0"
  },
  "main": "out/main/index.js",
  "description": "Cut silence from long recordings and get a ready-to-edit Premiere Pro project.",
  "author": "Schneilight"
}
